"""Prompt evaluation framework for e-commerce support classification.

The code is split into small modules:

- `llm_clients.py` calls the hosted models.
- `classifier.py` parses one model output.
- `runner.py` runs every prompt/model condition.
- `evaluator.py` calculates metrics and plots.
"""
